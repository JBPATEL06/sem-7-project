## Summary of Fixes Applied

1. **Top Submenu Dropdowns (`File`, `Edit`, `View`, `Object`, `Text`, `Arrange`)**:
   - Fixed dropdown z-index (`z-[9999]`) and clipping by adding `whitespace-nowrap`, `min-w-[220px]`, and `overflow-visible` to the sub-menu container.
   - Relocated the floating selection pill in `OpenPencilCanvas.tsx` from `top-7 left-7` to `bottom-4 left-4 z-20 pointer-events-none` so it never collides with or obscures the dropdown menus.
   - Removed the duplicate `Arrange` menu block from `ScreensPage.tsx`.

2. **New File Document Creation**:
   - Upgraded tab strip state (`openTabs`) to manage independent `SceneGraph` instances and `screenId` bindings for each open file.
   - Clicking `+` or `File -> New File` (Ctrl+Alt+N) creates a brand new document tab (e.g., `Untitled 2`) with a clean board and fresh canvas scene graph instead of appending layers to the existing document.
   - Added tab switching and closing logic.

3. **Settings and Grid Icons**:
   - **Settings Gear Icon**: Connected to `showDocSettingsModal` popup allowing document renaming (synced live to tab & screen), canvas background surface switching (`Light #F5F5F5`, `Dark #1E1E1E`, `Navy #0E131F`, `White #FFFFFF`), and grid toggle.
   - **Grid Icon**: Wired `showGrid={showGrid}` prop directly into `OpenPencilCanvas.tsx` with active indicator highlighting `#0d99ff` and toggleable dot grid rendering.

4. **Verification Proof**:
   - TypeScript compiler check: `npx tsc --noEmit` exited with code 0 (0 errors).
   - Unit tests: `npx vitest run tests/screensUi.test.ts` passed 3/3 tests.
   - Visual Browser Subagent verification executed:
     - `file_dropdown_open_1789560361627.png`: File menu verified open cleanly without clipping.
     - `document_settings_modal_1789560377630.png`: Document Settings modal verified functional.
     - `new_untitled_2_tab_1789560436177.png`: New document file creation verified with separate tab and clean canvas.
