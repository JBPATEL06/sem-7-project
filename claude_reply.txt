# Implementation Plan: Service Orchestration, Settings MCP Exposure, Semgrep QA, & GitHub-Style Git UI

I have created the implementation plan for the 4 requirements:

1. **Unified Dev Service Orchestration (`npm run dev`)**:
   - Update `packages/ai-manager-web/scripts/dev.js` to start Express (3000), Vite (5173), OpenPencil (1420), draw.io (8085), postgres-meta (1337), Supabase Studio (8082), and Git UI (3030) concurrently in order.
   - Verify all listening ports via `netstat`.

2. **Settings Page Master MCP Exposure ("Connect Your AI")**:
   - Add card in `SettingsPage.tsx` with live ngrok tunnel URL (1-click copy), Master MCP Bearer token (mask/reveal + copy), and ready-to-paste `mcpServers` JSON configuration for Claude Desktop / Antigravity clients.

3. **Real Semgrep Integration in QA Diagnostics**:
   - Add local Semgrep CLI / MCP integration in `semgrepService.ts` and `qaRoutes.ts`.
   - Add "Security & Code Quality Scan" tab in `QaPage.tsx` displaying real findings (rule ID, severity, file, line, fix suggestion). Keep existing AST checks in a separate tab.

4. **GitHub-Style Git UI (Gitea / Forgejo Integration)**:
   - Clone `go-gitea/gitea` to `packages/gitea-repo` (`git remote -v` + `git log` proof).
   - Configure local Git server serving workspace `.git` commit history.
   - Embed in `GitLineagePage.tsx` via `<iframe>` pointing to `http://localhost:3030`.

The plan is available at [implementation_plan.md](file:///C:/Users/jeel/.gemini/antigravity-ide/brain/bd35dc8c-1f78-4f82-a1ce-eafa45768abc/implementation_plan.md). Please review and approve to proceed with execution.
